import static io.println(int);

class Hello_T {	
	
	public static void main(String[] args){ 	
		boolean b;
		int i;
		i = 0;
		
		b = ( !(i<5 && !(3 < i && 2 < i))  && !(i<5 && !(3 < i && 2 < i) && i < 5) );
		
		
	}
	
 }