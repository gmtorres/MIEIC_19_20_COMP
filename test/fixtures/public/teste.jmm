import static BoardBase.sameArray(int[]) boolean;
import static BoardBase.sameArray(int[]) boolean;
import static BoardBase.printBoard(int[],int[],int[]) void; 
import static BoardBase.printWinner(int) boolean;

class TicTacToe {

	public boolean test(){
		int a;
		int [] b;
		boolean f;
		a = 2;
		b = new int[10];
		f = true;
		return b.length < 4 && BoardBase.printWinner(a);
	}
	
}
