import static BoardBase.sameArray(int[]) boolean;
import static BoardBase.sameArray(int[]) boolean;
import static BoardBase.printBoard(int[],int[],int[]) void; 
import static BoardBase.printWinner(int) void;
import static BoardBase.playerTurn(int) int[];
import static BoardBase.wrongMove();
import static BoardBase.placeTaken();

import static A.b(boolean);

class TicTacToe {

  int[] row0;
  int[] row1;
  int[] row2;
  int whoseturn;
  int movesmade;
  int p;
  int[] pieces;
  
  TicTacToe sape;
  
  // Initializes a Tic Tac Toe object.
  public boolean init() {
    row0 = new int[3];
    row1 = new int[3];
    row2 = new int[3];
    pieces = new int[2];
    pieces[0] = 1;
    pieces[1] = 2;
    whoseturn = 0;
    movesmade = 0;
    p = 1;
    
    sape = new TicTacToe();
    
    sape.getRow0(p, pieces[0]);
    
    return true;
  }
  
    public int[] getRow0(int a, int b){
    return row0;
  }

  public int[] getRow1(){
    return row1;
  }
  public int[] getRow2(){
    return row2;
  }
  
}
